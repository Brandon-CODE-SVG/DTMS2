spring.application.name=DTMS2

# Render will provide this via environment variable
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}

# JPA Configuration for production
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# Session Configuration
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always

# Server Configuration - Render provides PORT environment variable
server.port=${PORT:8080}
server.servlet.session.timeout=1800

# Production logging
logging.level.com.brandon.dtms2=INFO
logging.level.org.springframework=WARN

# CORS for production - update with your Render URL
app.cors.allowed-origins=postgresql://fitnessdb_5iw2_user:ZpTdhI43Z8nmn39RyWFPOXEnuGoRrBGR@dpg-d4jjll4hg0os73brtjk0-a.oregon-postgres.render.com/fitnessdb_5iw2
# Security - allow all origins in production for now (you can restrict later)
app.security.cors.allowed-origins=*